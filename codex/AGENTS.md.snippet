# 全局协作约定

## 朋友 skill — 与 Claude Code 协商

详细规则见 `~/.codex/skills/朋友/SKILL.md`。要点：

- **强制触发**：制定计划阶段、上下文压力大、任务复杂或有歧义、用户用了"重要/关键/小心"等高风险词、涉及破坏性操作
- **询问用户**：简单或难度难判的任务
- **手动触发**：用户输入 `/朋友` 或说"问问 claude / 和朋友商量 / 叫上朋友"
- **协商方式**：本机 `claude -p` 默认禁用（网关不通），反向走 `~/.shared/friend/codex_to_claude.md` ↔ `claude_to_codex.md` 文件邮箱，用户做邮差
- **多轮**：最多 5 轮，达不成一致则升级给用户
- **防递归**：收到带 `[FRIEND_CONSULT]` 的输入时，直接回复 AGREE/REFINE/OBJECT，不再反向调用 claude
