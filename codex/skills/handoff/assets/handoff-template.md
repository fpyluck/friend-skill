---
project_key: "{{project_key}}"
title: "{{title}}"
updated_at: "{{updated_at}}"
timezone: "{{timezone}}"
updated_by: "{{agent}}"
target_agent: "{{target_agent}}"
canonical_path: "{{project_root}}"
mailbox_root: "{{mailbox_root}}"
handoff_path: "{{handoff_path}}"
git_branch: "{{git_branch}}"
git_head: "{{git_head}}"
git_dirty: "{{git_dirty}}"
canonical_pointer: "{{canonical_pointer}}"
---

# {{title}} Handoff

## current_objective

- Background:
- Goal:
- Current stopping point:
- Out of scope:

## environment_commands

- Project root: `{{project_root}}`
- Execution environment:
- Virtual env/container:
- Run:
- Test:
- Build:
- Lint:
- Last verified: <command and result, or N/A>
- Unverified: <known gaps, or N/A>

## file_map

1. `{{project_root}}` - project root

## open_issues

1. N/A

## decisions_and_changes

1. N/A

## error_ledger

| Error / symptom | Root cause | Fix | Prevention |
|---|---|---|---|
| N/A | N/A | N/A | N/A |

## next_actions

1. N/A

## agent_notes

- For Codex:
- For Claude:
- Collaboration notes:
- External refs:

## owner_review

<!-- Annotate with [TODO] / [SUPPLEMENT] / [DONE] / [USER-ACTION]. AI: surface [TODO] and [USER-ACTION] to the user on reading. -->
- N/A

<!-- Handoff quality: absolute paths, one-line executable commands, <=5 items per section, no secrets. -->
